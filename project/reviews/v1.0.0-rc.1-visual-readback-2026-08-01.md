# v1.0.0-rc.1 visual readback

**Status:** Pass for repository-local release-candidate rendering
**Date:** 2026-08-01
**Scope:** Post-review lifecycle change and deterministic rerender

## Evidence

- Independent adversarial reviewer inspected all twelve PNG exports at candidate `fd24392530fa8ab19bc99ccba5ae27f1509dcf16` and reported no clipping, unreadable label, or missing major branch.
- The post-review visual diff changes only the lifecycle source, description, exports, metadata, and corresponding manifest hashes.
- `npm run render:visuals` regenerated all twelve exports successfully.
- The lifecycle PNG was opened at original resolution after rendering.

## Lifecycle result

The diagram visibly preserves all eleven canonical stages. `Improve` now reaches a distinct amber `Repeat?` human decision. The decision can begin a bounded cycle at `Discover`, enter the restartable safe pause, or end at `Intentional cycle stop`. Prioritize and Engage retain separate do-not-contact paths to `Durable human stop`; no arrow leaves that stop. Labels, shapes, and prose carry the meaning without depending on color alone.

The accessible description states the same restart, pause, intentional-stop, and durable do-not-contact semantics. Metadata contains the new description and synchronized source, SVG, PNG, and description hashes.

## Limit

This is an agent-assisted repository readback, not an independent human accessibility audit, assistive-technology test, or professional accessibility opinion. That qualified review remains a final-v1.0.0 gate.
